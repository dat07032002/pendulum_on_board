# SESSION LOG — 2026-06-26 (tilt project, full day)

Read this first, then `HANDOFF.md` for the broader project. This logs an intense day of
debugging the ±tilt training. **Headline: nominal swing-up+balance is SOLVED (0.97 verified
master); tilt-robustification is NOT, and we discovered the eval we were trusting is unreliable
under domain randomization.**

---

## 0. ⭐ START HERE — current state + immediate next actions

**Where we are:**
- ✅ **Nominal swing-up + balance is solved.** Best master `rl/models/nomB_fa_s0/best_model.zip`
  verifies at **0.97 free-arm / 0.87 cable** over 100 fresh nominal episodes. (Swing-up from
  hanging + balance on LEVEL ground, no tilt.) GIFs: `master_swingup.gif`, `tilt_balance.gif`.
- ❌ **Tilt-robustification is NOT solved.** Best ±20°-tilt policy (`tilt_fa20_s4`) verifies at
  only **~0.27** over 100 fresh episodes (the EvalCallback claimed 0.82 — see the critical finding).
- 🐛 **The DR eval is unreliable** — this is the most important discovery (§2.1). All tilt selection/
  stop decisions this session were fooled by an optimistic eval.

**▶ IMMEDIATE NEXT ACTIONS (in order):**
1. **STOP the 4 collapsed Step-2 runs** if still alive: `tilt_fa20_s0/s1/s2/s4` on GPUs 0/1/2/4
   (they peaked ~0.5 then collapsed to ~0.1). Delete cron job `231093a6`. Kill pattern:
   `for t in tilt_fa20_s0 tilt_fa20_s1 tilt_fa20_s2 tilt_fa20_s4; do for pid in $(pgrep -f "tag $t");
   do kill -9 -$(ps -o pgid= -p $pid|tr -d " "); done; done`
2. **FIX THE DR EVAL** (highest priority — without it we're blind). The EvalCallback `success_rate`
   AGREES with an independent 100-ep eval for NOMINAL (0.96 vs 0.97) but DIVERGES HUGELY with DR+tilt
   (0.82 vs 0.27). The DR eval is not sampling fairly. Investigate `furuta_env` DR + the eval env;
   build a fair fixed 100+-episode DR eval set. **Until fixed, trust ONLY the independent re-eval**
   (`rl/eval_policy.py`, see §4).
3. **Then** ease the difficulty + stabilize, and relaunch the warm-start tilt phase (Step 2):
   - Lower DR aggressiveness: `p_corner` 0.3 → ~0.1 (or narrow DR ranges to *measured* plant
     uncertainty). 30% worst-case-corner draws + tilt is near the ±6 V authority limit.
   - Try `top_quantiles_to_drop_per_net` 2 → 3 (counters TQC overestimation = the likely collapse
     cause; fixed ent_coef did NOT stop the collapse, so it's not entropy).
   - Keep warm-starting from `nomB_fa_s0/best_model.zip` (the 0.97 nominal master).
   - Drop the final-stage gate on `--stop_success` (§2.5).
4. **`no training till the user approves`** — see memory `approve-before-training`. Prepare + present,
   wait for an explicit "go".

---

## 1. What we did today (chronological)

1. **Entropy-collapse diag** (stage 0): `--no_sde` and `--target_entropy -2` both cross 0.6 @~130k
   and HOLD 1.0. Locked `--no_sde` as default (gSDE off).
2. **First full runs** (s0/s1/s2 + sde contrast, ±30° tilt, from scratch): entropy crept back up
   under DR (ent_coef → 0.5–0.8), all **peaked then collapsed**. Verified best ~0.40 free / 0.22 cable
   at ±30° tilt (EvalCallback said 0.78 — first sign the eval lies under DR).
3. **Project #1 forensics** (`../LQR_pendulum`): deployed model used **gSDE ON + target_entropy −1,
   ent_coef ~0.2**. Its leftover tb/eval logs are all FAILED attempts showing the SAME entropy-creep
   + collapse. Steps-to-peak for the deployed model wasn't preserved (~0.7 M per old handoff).
4. **Geometry explainers**: orientation-dependent tilt coupling; arm-angle-limit. **CORRECTION**: at
   φ=0 (benign) the **arm is PERPENDICULAR to the tilt axis** (pole swings ALONG the axis). Earlier I
   wrongly said "parallel" — see §3.
5. **Swing-up failure hunt**: hypothesized the 0.20 arm-centering penalty strangled the pump; lowered
   to 0.02 → did NOT fix the stall. Then found via eval+keep-best that a **0.91 nominal master existed
   all along** (`nomA_fa_s0`) — the *curriculum* success metric was misleading; the policy could swing
   up, the gate logic just looked stuck.
6. **Two-phase warm-start strategy adopted**: Phase A = nominal pretrain (no DR), Phase B = warm-start
   + robustify (DR+tilt) with FIXED ent_coef (kill the auto-tuner creep).
7. **Phase B (±30° from nomA master)**: warm-start raced to stage 7 fast, EvalCallback ~0.78, but
   **verified only 0.40 free / 0.22 cable** (the eval-lies-under-DR pattern again).
8. **Added tooling**: `--stop_success` (early-stop at an eval threshold → capture peak, skip collapse),
   `--n_eval`. **Removed arm-centering for free-arm**: `arm_center_w 0` AND the `|φ|<90°` requirement in
   bonus/success is now gated on `arm_limit` (cable only).
9. **Step 1 — fresh nominal masters** (5 free-arm seeds, no centering, stop@0.95, n_eval=100):
   s0 auto-stopped at 0.96 (verified **0.97**); see §4 table. 4/5 seeds reached ≥0.78 nominal — the
   no-centering reward makes nominal reliably learnable (old version: 3/4 stalled at stage 0).
10. **Step 2 — warm-start to ±20° tilt** (4 seeds 0/1/2/4 from their own masters, stop@0.80, n_eval=100):
    **peaked then collapsed AGAIN despite fixed ent_coef.** EvalCallback said s4=0.82 / s0=0.72, but
    **independent re-eval: s4=0.27, s0=0.11.** → the critical eval finding (§2.1).

---

## 2. KEY FINDINGS

### 2.1 🐛 THE DR EVAL IS UNRELIABLE (most important)
- Independent 100-ep eval AGREES with the SB3 EvalCallback for **nominal** (no DR): 0.96 ≈ 0.97.
- They DIVERGE massively once **DR + tilt** is on: EvalCallback 0.82 vs independent **0.27**.
- Only difference is domain randomization → the EvalCallback DR eval gets a lucky/narrow plant slice,
  while a fair 100-ep draw (incl. 30% corner-weighted worst-case plants) is far harder.
- **Consequence:** every DR-based best-model selection AND every `--stop_success` decision this session
  was made on inflated numbers. This explains the whole session's "good number that doesn't hold up"
  pattern. **ALWAYS re-verify tilt policies with `rl/eval_policy.py` (independent fresh seeds).**

### 2.2 Peak-then-collapse is NOT entropy
- It recurs with `ent_coef` FIXED at 0.05 (auto-tuner disabled, verified). So it's not entropy.
- Most likely **TQC critic value overestimation**. Next lever: `top_quantiles_to_drop_per_net` 2→3
  (more conservative critic). Keep-best + `--stop_success` *mitigate* (capture peak) but don't *cure*.

### 2.3 Real difficulty
- Verified tilt performance is low (±30°: ~0.40 free; ±20°: ~0.27 free) — the corner-weighted DR
  (`p_corner=0.3`) + tilt is near the ±6 V authority limit. Policies handle easy draws, fail corners.
- **cable + tilt ≫ harder than free-arm + tilt** (holding true-vertical under tilt drifts the arm into
  the ±180° cable). Raising `arm_center_w` to 0.15 for the cable helped *training-eval* but real perf
  stayed low. Free-arm is sim-only (real rig has the cable).

### 2.4 What works
- **Nominal swing-up+balance is solved & robust** (0.97/0.87). The two-phase split is the right
  structure: learn the skill cleanly (no DR) → robustify (warm-start). Warm-start is FAST.
- **`--stop_success` + no-centering + n_eval=100** gave a clean nominal master with no wasted compute
  (s0 stopped at 280k @0.96). Multi-seed + keep-best handles the high seed variance.

### 2.5 `--stop_success` gating bug (minor)
- It only fires at the final curriculum stage (`max_stage`). But the eval env is ALWAYS at the target
  tilt regardless of curriculum stage, so a warm-started policy can hit threshold *early* (s4 hit 0.82
  on the eval at step 60k while still ramping) and the stop won't fire. **Fix: drop the stage gate**
  (stop whenever the target-condition eval crosses the threshold). Keep-best covered for it this time.

### 2.6 Geometry (CORRECTED) — for Phase-2 hardware
- φ=0 (cable-center, benign): **arm ⟂ tilt axle**, pole swings ALONG the axle (tilt can't drive it).
- φ=90° (hard): arm ∥ axle, pole swings ⟂ (tilt drives it max).
- **Mount the real rig so the arm-rest/zero points PERPENDICULAR to the LX-16A tilt axle.** (Sim is
  already correct; only my earlier verbal advice was inverted.)

---

## 3. Models / verified results (100-ep INDEPENDENT eval — the trustworthy numbers)

**Nominal masters** (Step 1: free-arm, arm_center_w 0, no DR; `rl/models/nomB_fa_s*/best_model.zip`):
| seed | free-arm | cable | note |
|---|---|---|---|
| **0** | **0.97** | **0.87** | ⭐ THE MASTER — warm-start everything from this |
| 1 | 0.89 | 0.88 | most cable-robust |
| 2 | 0.88 | 0.60 | free-arm only |
| 4 | 0.78 | 0.63 | borderline |
| 3 | 0.63 | 0.04 | DROPPED — over-rotates the arm |

(Older nominal master `nomA_fa_s0`: 0.91/0.86, arm_center_w 0.02 — superseded by nomB_fa_s0.)

**Tilt policies** (Step 2: warm-start, ±20° tilt — VERIFIED, not the inflated EvalCallback):
| run | EvalCallback "best" | independent ±20°tilt+DR | 
|---|---|---|
| tilt_fa20_s4 | 0.82 | **0.27 free / 0.11 cable** |
| tilt_fa20_s0 | 0.72 | **0.11 free / 0.06 cable** |
(Earlier ±30° `tilt_fa_s0`: independent 0.40 free / 0.22 cable.)

---

## 4. Code changes today (all committed)

Commits: `f783738` (gSDE off default) · `046ccb9` (two-phase warm-start) · `067b6d1` (arm_center_w
configurable) · `a17b2db` (--stop_success + free-arm no-centering). All on `main`, local only (no
remote — ask before pushing).

`rl/train_tqc.py` new flags:
- `--no_sde`/`--sde` (gSDE off default), `--no_dr` (Phase A nominal, stages 0–4),
  `--warmstart <zip>` (load policy weights into a fresh model), `--start_stage`/`--max_stage`,
  `--free_arm` (remove ±180 cable termination), `--arm_center_w` (centering weight; 0 = none),
  `--ent_coef`/`--target_entropy`, `--lr`, `--eval_tilt_deg`, `--stop_success <thresh>`, `--n_eval`.
- `Curriculum(start_stage, max_stage, force_no_dr)`; `StopOnSuccess` callback (gated on final stage —
  see §2.5, drop the gate).

`rl/furuta_env.py`:
- `self.arm_limit` (None = free arm), `self.arm_center_w` (default 0.20).
- reward uses `self.arm_center_w * (phi/π)²`; cable termination guarded by `arm_limit is not None`.
- `|φ|<90°` in bonus/success now gated: `arm_ok = arm_limit is None or |φ|<π/2` (free arm → no bound).

`rl/eval_policy.py` (NEW — the trustworthy independent eval; USE THIS to verify any policy):
- `python rl/eval_policy.py <model.zip> [--tilt_deg 0|20|30] [--dr] [-n 100]` → prints free-arm &
  cable success over N fresh episodes. Nominal (tilt 0, no --dr) matches EvalCallback; tilt+--dr is the
  honest deployment metric that the EvalCallback over-reports.

`rl/diag_stage0.py`: `--randomize --init_angle --eval --free_arm` (stage-isolation diag harness).

---

## 5. Server / infra state (UT: tn22833@aere-a83514, ~/furuta_tilt, venv ~/furuta_rl/.venv)
- 5× RTX 6000 Ada, SHARED (other users' jobs appear intermittently). Launch with
  `CUDA_VISIBLE_DEVICES=N setsid nohup ... < /dev/null &` (setsid detaches; survives the ssh session).
  Note: killed-run ssh sessions emit stale exit-255 notifications for a long time — ignore them.
- Logs have NULL BYTES (eval prints) → read with `strings <log> | grep`, not plain grep.
- As of session end: 4 collapsed Step-2 runs likely still on GPUs 0/1/2/4 (STOP them, §0.1).
  Cron `231093a6` monitoring Step 2 (delete it). Masters saved under `rl/models/nomB_fa_s*/`.
- Local artifacts: `master_swingup.gif`, `tilt_balance.gif` (project root). Eval/gif scripts also in
  the session scratchpad (temp — `rl/eval_policy.py` is the persisted one).

## 6. The big open question
Is ±20°/±30° tilt under the CURRENT corner-DR even achievable on ±6 V? Phase-0 said ±30° is *marginal*
at the worst orientation. With an honest eval we may find the real ceiling is ~0.4–0.6, forcing a
trade between tilt range and DR aggressiveness. **Fix the eval first so we can actually measure it.**
